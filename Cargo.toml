[workspace]
members = ["src/libkrun", "src/libkrun-cdylib", "src/krun_input"]
exclude = ["examples/gtk_display"]
resolver = "2"

[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true
